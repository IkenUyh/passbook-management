<UserControl x:Class="frontend_csharp.UserControls.AccountManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="640" d:DesignWidth="790">
    <UserControl.Resources>
        <Style x:Key="InputPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="White" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OldPasswordStyle" TargetType="PasswordBox" BasedOn="{StaticResource InputPasswordBoxStyle}">
            <Setter Property="BorderBrush" Value="#EF4444"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding OldPasswordError}" Value="">
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding OldPasswordError}" Value="{x:Null}">
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NewPasswordStyle" TargetType="PasswordBox" BasedOn="{StaticResource InputPasswordBoxStyle}">
            <Setter Property="BorderBrush" Value="#EF4444"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding NewPasswordError}" Value="">
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding NewPasswordError}" Value="{x:Null}">
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ConfirmPasswordStyle" TargetType="PasswordBox" BasedOn="{StaticResource InputPasswordBoxStyle}">
            <Setter Property="BorderBrush" Value="#EF4444"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ConfirmPasswordError}" Value="">
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ConfirmPasswordError}" Value="{x:Null}">
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GreenButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EF4444"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Height" Value="16"/>
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="Visibility" Value="Hidden"/>
                </Trigger>
                <Trigger Property="Text" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Hidden"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Border x:Key="ChangePasswordSuccessPopupUI" Width="450" Background="White" CornerRadius="12" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <materialDesign:PackIcon Kind="CheckCircle" Foreground="#10B981" Width="28" Height="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Đổi mật khẩu thành công!" FontSize="20" FontWeight="Bold" Foreground="#111827" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,24">
                    <StackPanel>
                        <TextBlock Text="Thông tin tài khoản hệ thống:" Foreground="#6B7280" FontSize="13" Margin="0,0,0,12" FontStyle="Italic"/>

                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Tài khoản đăng nhập:" FontWeight="SemiBold" Foreground="#374151" FontSize="13"/>
                            <TextBlock Text="{Binding Username}" HorizontalAlignment="Right" Foreground="#111827" FontWeight="Bold" FontSize="13"/>
                        </Grid>

                        <Grid>
                            <TextBlock Text="Quyền hạn hiện tại:" FontWeight="SemiBold" Foreground="#374151" FontSize="13"/>
                            <TextBlock Text="{Binding Role}" HorizontalAlignment="Right" Foreground="#3A5EEA" FontWeight="Bold" FontSize="13"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource GreenButtonStyle}" Content="Hoàn tất" Click="CloseSuccessPopup_Click" Width="120"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Thiết lập tài khoản" FontSize="20" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,5"/>
            <TextBlock Text="Quản lý thông tin hồ sơ nhân viên và bảo mật mật khẩu hệ thống" FontSize="13" Foreground="#6B7280"/>
        </StackPanel>

        <Grid Grid.Row="1" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="22" Background="#FBFBFB">
                <StackPanel>
                    <TextBlock Text="Thông tin cá nhân" FontSize="15" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="Tài khoản hệ thống" FontSize="12" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding Username}" FontSize="14" FontWeight="Bold" Foreground="#111827"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="Quyền hạn" FontSize="12" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding Role}" FontSize="14" FontWeight="SemiBold" Foreground="#3A5EEA"/>
                    </StackPanel>

                    <Separator Background="#E5E7EB" Margin="0,5,0,15"/>

                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Account" Foreground="#9CA3AF" Width="18" Height="18" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Họ và tên" FontSize="11" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding FullName}" FontSize="14" Foreground="#374151" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="CardAccountDetails" Foreground="#9CA3AF" Width="18" Height="18" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Số định danh (CCCD)" FontSize="11" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding CitizenId}" FontSize="14" Foreground="#374151"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Phone" Foreground="#9CA3AF" Width="18" Height="18" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Số điện thoại liên hệ" FontSize="11" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding PhoneNumber}" FontSize="14" Foreground="#374151"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="22" Background="#FBFBFB">
                <StackPanel>
                    <TextBlock Text="Thay đổi mật khẩu" FontSize="15" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,3"/>
                    <TextBlock Text="Đảm bảo mật khẩu sử dụng ký tự an toàn" FontSize="12" Foreground="#6B7280" Margin="0,0,0,12"/>

                    <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ErrorTextStyle}" FontWeight="SemiBold" Margin="0,4,0,12" Height="16"/>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Mật khẩu hiện tại" FontSize="13" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="PbOldPassword" Style="{StaticResource OldPasswordStyle}"/>
                        <TextBlock Text="{Binding OldPasswordError}" Style="{StaticResource ErrorTextStyle}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Mật khẩu mới" FontSize="13" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="PbNewPassword" Style="{StaticResource NewPasswordStyle}"/>
                        <TextBlock Text="{Binding NewPasswordError}" Style="{StaticResource ErrorTextStyle}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Xác nhận mật khẩu mới" FontSize="13" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="PbConfirmPassword" Style="{StaticResource ConfirmPasswordStyle}"/>
                        <TextBlock Text="{Binding ConfirmPasswordError}" Style="{StaticResource ErrorTextStyle}"/>
                    </StackPanel>

                    <Button Style="{StaticResource GreenButtonStyle}" Content="Cập nhật mật khẩu" Click="ConfirmChangePassword_Click" Height="40" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>