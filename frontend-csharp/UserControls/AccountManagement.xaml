<UserControl x:Class="frontend_csharp.UserControls.AccountManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="640" d:DesignWidth="790">
    <UserControl.Resources>
        <Style x:Key="InputPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="White" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GreenButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề lớn -->
        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Thiết lập tài khoản" FontSize="22" FontWeight="Bold" Foreground="#111827"/>
            <TextBlock Text="Quản lý thông tin hồ sơ nhân viên và bảo mật mật khẩu hệ thống" FontSize="13" Foreground="#6B7280" Margin="0,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Cột trái: Thông tin tài khoản -->
            <Border Grid.Column="0" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="20" Background="#FBFBFB" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Thông tin cá nhân" FontSize="15" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="Tài khoản hệ thống" FontSize="12" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding Username}" FontSize="14" FontWeight="Bold" Foreground="#111827"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="Quyền hạn" FontSize="12" Foreground="#9CA3AF" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding Role}" FontSize="14" FontWeight="SemiBold" Foreground="#3A5EEA"/>
                    </StackPanel>

                    <Separator Background="#E5E7EB" Margin="0,5,0,15"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Account" Foreground="#9CA3AF" Width="18" Height="18" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Họ và tên" FontSize="11" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding FullName}" FontSize="14" Foreground="#374151" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="CardAccountDetails" Foreground="#9CA3AF" Width="18" Height="18" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Số định danh (CCCD)" FontSize="11" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding CitizenId}" FontSize="14" Foreground="#374151"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Phone" Foreground="#9CA3AF" Width="18" Height="18" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Số điện thoại liên hệ" FontSize="11" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding PhoneNumber}" FontSize="14" Foreground="#374151"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Cột phải: Đổi mật khẩu -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="Thay đổi mật khẩu" FontSize="15" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,5"/>
                <TextBlock Text="Đảm bảo mật khẩu sử dụng các ký tự an toàn để bảo mật" FontSize="12" Foreground="#6B7280" Margin="0,0,0,15"/>

                <!-- Thông báo lỗi nghiệp vụ dạng mẫu chung của dự án -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" FontSize="12" Margin="0,0,0,10" FontWeight="SemiBold">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="Mật khẩu hiện tại" FontSize="13" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PbOldPassword" Style="{StaticResource InputPasswordBoxStyle}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="Mật khẩu mới" FontSize="13" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PbNewPassword" Style="{StaticResource InputPasswordBoxStyle}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="Xác nhận mật khẩu mới" FontSize="13" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PbConfirmPassword" Style="{StaticResource InputPasswordBoxStyle}"/>
                </StackPanel>

                <Button Style="{StaticResource GreenButtonStyle}" Content="Cập nhật mật khẩu" Click="ConfirmChangePassword_Click" Height="40" FontSize="13" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>